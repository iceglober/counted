{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "deploy/api.Dockerfile",
    "_comment": "No watchPatterns. They filter `railway up` as well as git pushes, so a deploy from an unchanged tree comes back SKIPPED \u2014 which reads in the dashboard like a deploy that worked."
  },
  "deploy": {
    "healthcheckPath": "/health/ready",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3,
    "numReplicas": 2,
    "overlapSeconds": 30,
    "drainingSeconds": 20
  }
}
