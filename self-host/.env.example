# Required
BETTER_AUTH_SECRET=   # openssl rand -base64 32
BASE_URL=http://localhost:3000

# Database (change in production)
POSTGRES_PASSWORD=counted

# Email (optional — magic links work without, check DB for tokens)
RESEND_API_KEY=
RESEND_FROM=
