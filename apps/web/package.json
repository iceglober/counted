{
  "name": "@counted/web",
  "version": "0.0.0",
  "private": true,
  "description": "The console. A contract client and a forwarding proxy — it holds no credential of its own.",
  "type": "module",
  "dependencies": {
    "@counted/contract": "workspace:*",
    "@counted/openapi": "workspace:*",
    "@counted/ui": "workspace:*",
    "@tabler/icons-react": "^3.46.0",
    "cn": "^0.2.5",
    "react-grid-layout": "2.2.3",
    "react-resizable": "3.1.3",
    "recharts": "3.8.0"
  },
  "scripts": {
    "test": "bun test src",
    "dev": "next dev",
    "build": "bun ../../scripts/generate-openapi.ts && next build",
    "start": "next start --port 3000"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "4.3.3",
    "tailwindcss": "4.3.3"
  },
  "imports": {
    "#components/*": "./src/components/*.tsx",
    "#lib/*": "./src/lib/*.ts",
    "#hooks/*": "./src/hooks/*.ts"
  }
}
