import type { Metadata } from "next";
import { SiteArticle } from "../../../components/site-chrome";
export const metadata: Metadata = { title: "About", alternates: { canonical: "/about" }, description: "Privacy-first product analytics, built by Iceglobe Enterprises LLC. Open source and self-hostable." };
export default function About() { return <SiteArticle title="About Counted"><p>Counted is privacy-first product analytics. It captures custom events, funnels, and composable dashboards without tracking cookies or fingerprinting.</p><p>It is built by <strong>Iceglobe Enterprises LLC</strong>, a US company, and is open source. You can read the code and self-host it with Docker Compose and plain PostgreSQL.</p><p>The SDK keeps an ephemeral visit id in memory. It does not write a tracking identifier to a cookie, local storage, or disk. Counted never derives a person’s identity; an optional opaque identifier comes only from your explicit call to <code>identify()</code>.</p><h2>Built for apps and agents</h2><p>Count events, follow trends, break results down by properties, and measure ordered funnels. Arrange the resulting Insights on a dashboard, or work directly with the API.</p><p>The same event model instruments AI coding agents through integrations for Claude Code, OpenCode, and stdin hooks for other hosts.</p><p><a href="/docs">Read the documentation</a> · <a href="/pricing">View plans</a> · <a href="https://github.com/iceglober/counted">Read the source</a></p></SiteArticle>; }
