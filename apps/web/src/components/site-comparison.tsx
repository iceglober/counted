import { Card, CardHeader, CardTitle, CardDescription } from "@counted/ui/components/card";
import { PageIntro } from "./site-chrome";
import { COMPARISONS } from "../lib/public-content";
export function SiteComparison({ slug }: { slug: (typeof COMPARISONS)[number]["slug"] }) {
  const item = COMPARISONS.find(candidate => candidate.slug === slug)!;
  return <><PageIntro eyebrow="Compare" title={`Counted vs ${item.name}`}>{item.description}</PageIntro><div className="grid items-start gap-8 lg:grid-cols-[1.4fr_1fr] lg:gap-14"><article className="site-prose"><h2 className="!mt-0">What Counted provides</h2><p>Counted is privacy-first product analytics: custom events, ordered funnels, and dashboards you compose yourself.</p><ul><li>Use an event’s properties as filters or breakdown dimensions.</li><li>Combine counts, time series, breakdowns, and funnels as Insights on a dashboard.</li><li>Keep analytics visits ephemeral, without tracking cookies or fingerprinting.</li><li>Work through the app, the API, or SDKs. Instrument AI coding agents using the same event model.</li><li>Read the MIT-licensed source or self-host with Docker Compose and PostgreSQL.</li></ul>{slug === "counter" && <p>Counted and counter.dev are different products by different people. The similar names do not indicate an affiliation.</p>}<p><a href="/docs">Explore the API</a> or <a href="/pricing">see Counted’s plans</a> for the supported features and limits.</p></article><Card><CardHeader><CardTitle>Explore {item.name}</CardTitle><CardDescription>Features and plans change. Read {item.name}’s current product information to compare it with the capabilities you need.</CardDescription><a href={item.url} className="mt-3 text-sm text-primary-ink underline underline-offset-4">Visit {item.name} <span aria-hidden="true">↗</span></a></CardHeader></Card></div></>;
}
