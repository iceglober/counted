{
  "name": "@counted/contract",
  "version": "0.0.0",
  "private": true,
  "description": "The oRPC v2 contract. One description of every route, consumed by the server, the generator, the MCP layer and every client.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@counted/kernel": "workspace:*"
  },
  "scripts": {
    "test": "bun test src"
  }
}
