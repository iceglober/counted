/**
 * @counted/ports — the interfaces the domain talks through, and the contract
 * suites every adapter must pass.
 *
 * A port lives here; an implementation never does.
 */

export const PORTS_LAYER = "counted-ports" as const;
