/**
 * Shared contract suites. An adapter is only "done" when it passes these.
 *
 * Populated by #31 (AnalyticalStore, EventWriter, repositories, cache) and the
 * bucket differential test that pins the domain time axis to SQL assignment.
 */

export const CONTRACT_SUITES = [] as const;
