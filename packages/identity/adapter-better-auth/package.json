{
  "name": "@counted/identity-adapter-better-auth",
  "version": "0.0.0",
  "private": true,
  "description": "better-auth behind the identity ports. The only package that may import better-auth or @better-auth/*.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@counted/identity-ports": "workspace:*",
    "@counted/kernel": "workspace:*"
  },
  "scripts": {
    "test": "bun test src"
  }
}
