{
  "name": "@counted/projects-domain",
  "version": "0.0.0",
  "private": true,
  "description": "Project aggregate and every rule about credentials — rotation overlap, last-key, grantable permission set.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@counted/kernel": "workspace:*"
  },
  "scripts": {
    "test": "bun test src"
  }
}
