/**
 * @counted/application — use cases, planning, and policy.
 *
 * Depends on @counted/domain and @counted/ports. Never on an adapter.
 */

export const APPLICATION_LAYER = "counted-application" as const;
