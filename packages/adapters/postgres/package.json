{
  "name": "@counted/adapter-postgres",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Postgres implementation of the storage ports. Plain PostgreSQL; TimescaleDB optional.",
  "exports": { ".": "./src/index.ts" },
  "scripts": { "build": "tsc --build", "test": "bun test", "typecheck": "tsc --noEmit" },
  "dependencies": {
    "@counted/domain": "workspace:*",
    "@counted/ports": "workspace:*",
    "pg": "^8.13.0"
  }
}
