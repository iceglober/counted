/**
 * @counted/adapter-crypto — the non-determinism the domain forbids itself.
 *
 * Secrets, digests and identifiers. Nothing here knows what a credential is
 * *for*; scopes and lifecycle belong to the Project aggregate.
 */
export * from "./secrets";
export * from "./ids";
