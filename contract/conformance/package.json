{
  "name": "@counted/conformance",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The cross-language SDK contract: numbered behaviour, scripted scenarios, one runner per language.",
  "scripts": { "test": "bun test" },
  "dependencies": { "@counted/sdk-js": "workspace:*" }
}
