---
---

Repair the existing 2.0.0 npm release artifacts: publish the JavaScript implementation as @counted/sdk, include the agent dependency package, and verify clean package installation. The version is already set on each package.
